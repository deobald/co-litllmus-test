// mixed var definitions and assignments across if/while in same scope
var x = 0;
if (true) {
  var y = 1;
  x = x + y;
}
while (x < 3) {
  var z = 10;
  x = x + 1;
}
print(x); // 3
print(y); // 1
print(z); // 10
